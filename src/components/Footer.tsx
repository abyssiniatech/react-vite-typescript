

const Footer = () => {
  return (
    <div>
        <footer className="h-screen flex justify-center  items-center bg-indigo-900 text-white text-center">
         <h2>&copy; {new Date().getFullYear()} | Surafel mengist</h2>
        </footer>
    </div>
  )
};

export default Footer;